- name: install nginx and git
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install nginx and git
    ansible.builtin.package:
      name: "{{ item }}"
      state: installed


    loop:
    - nginx
    - git

  