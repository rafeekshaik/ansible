- name: loops
  hosts: local
  connection: local
  tasks:
  - name: print the names using loops
    ansible.builtin.debug:
       msg: "hello {{ item }}"
    


    loop:
    - venkatesh
    - ramesh
    - suresh