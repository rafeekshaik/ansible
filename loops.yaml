- name: installing packages
  hosts: local
  connection: local
  tasks:
  - name: installing packages
    ansible.builtin.package:
        name: "{{ item }}"
        state: installed

    loop:
    - nginx
    - git
    - zip
    - gcc