- name: filters
  hosts: local
  connection: local
  tasks:
  - name: filters
    ansible.builtin.debug:
      msg: "hello {{ person | default('ramesh') }}"

  - name: convert srings into list
    vars:
      fruits: "apple, banana, orange"
    ansible.builtin.debug:

       msg: " fruits are {{ fruits | split(',') }}"

  - name: convert map into list
    vars:
      course:
        name: ansible 
        duration: 120
        trainer: sivakumar
    ansible.builtin.debug:
      msg: "{{ course | dict2items }}"